(function (angular) {
    // "use strict";
    // start your ride
    var app = angular.module('main', [
     // 路由规则，先引用，先匹配
      'home',
      'details',
      'movie_list',
      'auto-active'
      ])
})(angular);